---
# - name: Ignite
#   hosts: WORKSTATION
#   connection: local
#   gather_facts: yes
#   become: no

  # tasks:
  # - name: Debug
  #   ansible.builtin.debug:
  #     msg:
  #     - "Hello"

  # - name: Facts
  #   ansible.builtin.debug:
  #     var: ansible_facts

- name: Install MinIO server
  # hosts: all
  hosts: WORKSTATION
  connection: local
  become: no

  roles:
    - role: dzintars.minio
      vars:
        minio_domain: 's3.oswee.com'
      tags: minio
    - role: local.minio
